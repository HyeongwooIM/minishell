#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include "test.h"

int main()
{
	// int pid;
	// g_int = 0;
	// printf("%d\n", g_int);
	// int i = 20;
	// pid = fork();
	// while (1)
	// {
	// 	sleep(1);
	// 	i--;
		
	// 	if (pid == 0)
	// 	{
	// 		printf("child g_int %d\n", g_int);
	// 		g_int++;
	// 	}
	// 	else
	// 		printf("pa g_int %d\n", g_int);
	// 	if (i == 0)
	// 		break;
	// }
	char *str = 0;
	free(str);
	return (0);
}